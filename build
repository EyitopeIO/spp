#!/bin/bash

set -Ee

while : ; do
  option="$1"
  #echo "option is $option"
  case "$option" in
  -d)
      export DEBUG=y
      shift
      ;;
  -t)
      DO_TESTS=y
      shift
      ;;
  *)
     break
     ;;
  esac
done

mkdir -p cbuild
cd cbuild
cmake -DCMAKE_BUILD_TYPE=Debug ../
make
cd ../

if [ -n "$DO_TESTS" ]; then
  make tests
  tests/runtests
fi